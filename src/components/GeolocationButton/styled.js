import styled from "styled-components";
import { BaseButton } from "../../styles/base";

export const GeolocationButton = styled(BaseButton)`
  position: fixed;
  z-index: 2;
  bottom: 10px;
  border: 0;
  right: 20px;
`;
