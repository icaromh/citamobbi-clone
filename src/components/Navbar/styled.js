import styled from "styled-components";

export const Navbar = styled.nav`
  z-index: 1;
  position: sticky;
  display: flex;
  min-height: 50px;

  padding: 5px 10px;

  top: 0;
  left: 0;
  right: 0;
`;
